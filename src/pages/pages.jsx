import React from 'react';
import Slider from './Slider'; // Use a capital 'S' for Slider component

const Pages = () => {
  return (
    <div>
      <Slider /> {/* Use a capital 'S' for Slider component */}
    </div>
  );
};

export default Pages; // Use a capital 'P' for Pages component
